/**
 * choose key
 * 1. avoid search between DB
 * 2. try to average data set
 */

/**
 * Partition method
 * 1. Hash the key and % ->
 * 2. Base on the range of key
 * 3. key and page map ->
 */

/**
 * UUID
 * one data point one partition:
 * 1.auto_increment_increment and auto_increment_offset
 *
 * 2. global node to generate ID
 *
 * 3. Redis create global ID
 */

/**
 * oneProxyp
 */